切换屏幕显示服务器阻止状态
某些 %PRODUCTNAME% 功能(例如帧率监控、屏幕显示渲染、屏幕截图和自动配置文件管理)依赖于附加的 %SERVERPRODUCTNAME% 进程。因此,当您启用上述任何功能时,%PRODUCTNAME% 会自动启动 %SERVERPRODUCTNAME%。
但是,某些在线游戏的反作弊系统可能会限制屏幕显示的使用,并在 %SERVERPRODUCTNAME% 运行时阻止与游戏服务器的连接。
为了简化此类问题的诊断,您可以使用热键临时阻止 %SERVERPRODUCTNAME% 并将其从内存中卸载,即使 %PRODUCTNAME% 的正常功能需要该服务器。请注意,当服务器被阻止并卸载时,所有依赖于服务器的功能都将停止工作。

提示:
- 某些热键组合(例如 <1>...<5>、<Ctrl>+<1>...<Ctrl>+<5> 和 <Ctrl>+<D>)为保留组合,无法使用。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
