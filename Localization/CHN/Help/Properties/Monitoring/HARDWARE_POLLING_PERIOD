硬件轮询周期
调整硬件轮询和监控图表更新周期。

提示:
- 硬件轮询并非完全无性能损耗，每次轮询迭代都需要一些 CPU 时间从硬件读取数据。根据 GPU 和传感器型号以及硬件访问协议，硬件轮询相关的性能损耗从几乎不可见到明显的周期性性能损耗不等，后者会在密集使用 CPU 的应用程序（如 3D 游戏）中造成卡顿现象。
如果您在运行 %PRODUCTNAME% 时遇到此类现象，请尝试暂时暂停监控以确认其为问题来源，方法是右键点击监控窗口并选择<暂停>命令。然后尝试通过增加硬件轮询周期或禁用不常用或非必要的活动硬件监控图表来降低 CPU 负载。
有经验的用户还可以右键点击监控窗口并选择<显示状态>命令，在监控窗口中显示性能分析器状态信息。状态信息显示不同后台进程（包括硬件轮询）的 CPU 使用时间。
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
