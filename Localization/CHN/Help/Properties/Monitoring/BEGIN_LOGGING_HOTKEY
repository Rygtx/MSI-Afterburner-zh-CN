开始记录热键
允许定义全局热键,用于开始将硬件监控历史记录到文件。

提示：
- %PRODUCTNAME% 以原生的 RivaTuner 日志文件格式保存历史记录。您可以使用内置查看器或 RivaTuner 来查看用 %PRODUCTNAME% 录制的日志文件。
- 硬件监控历史记录在性能方面并非完全无开销,每次日志更新迭代都需要一些 CPU 时间将数据刷新到硬盘。
- 某些热键组合(例如 <1>...<5>、<Ctrl>+<1>...<Ctrl>+<5> 和 <Ctrl>+<D>)已被保留,无法使用。
- 您可以通过高级属性中的 <User interface> 选项卡禁用用户界面工具提示。
