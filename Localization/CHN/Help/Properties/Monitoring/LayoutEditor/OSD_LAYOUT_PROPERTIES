活动的 On-Screen Display 布局属性
显示当前活动的 On-Screen Display 布局的属性列表,并允许对其进行自定义。

提示：
- 您可以通过双击属性,或选中属性并按 <Enter> 键来更改属性。
- 如果在双击某些属性时按住 <Ctrl> 键,您可以更改与之关联的附加设置。例如,您可以在 <Value alignment> 节点中双击属性时按住 <Ctrl> 键,以在固定字体大小和可变字体大小对齐模式之间切换。
- 您可以通过选中根节点或其子项之一并按 <Insert> 键,向某些属性节点(例如颜色库)添加子项。
- 您可以通过选中所需项目并按 <Delete> 键,从某些属性节点(例如颜色库)中删除子项。
- 您可以通过高级属性中的 <User interface> 选项卡禁用用户界面工具提示。
