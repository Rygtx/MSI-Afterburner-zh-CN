擦除自动保存的启动设置
AMD Crimson 驱动程序系列会尝试自动将硬件设置(例如超频或风扇控制设置)保存到注册表中,并尝试在 Windows 启动时应用这些设置。这使得任何第三方硬件超频工具的设置实际上是永久性的,这意味着应用的设置即使在重新启动或关闭系统后仍会保留在系统中。然而,这种实现方式是不安全的,甚至存在潜在危险,因为您再也无法通过系统重启甚至驱动程序重新安装来撤销不安全的超频。为防止这种情况,%PRODUCTNAME% 可以在将新设置应用到硬件后选择性地擦除 AMD 驱动程序自动保存的启动设置。

提示:
- 仅当系统中安装了至少一块受支持的 AMD 显卡时,此选项才处于活动状态。
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
