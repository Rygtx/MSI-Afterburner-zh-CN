图形处理器列表
显示系统中安装的图形处理器列表，允许选择主 GPU。您在主应用程序窗口中所做的所有 GPU 调整都将应用到主 GPU，并且在启用了相邻的图形处理器设置同步选项时，也会应用到系统中安装的所有相似 GPU。

提示:
- 在切换到新的主 GPU 之前，请始终应用您在主应用程序窗口中所做的更改。选择新的主 GPU 时，任何未应用的 GPU 设置都会丢失。
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
