音频源类型
允许选择音频源类型。您最多可以选择两个独立的音频源，并在最终视频中录制两条独立的音轨。可用的音频源类型如下:

- 自动选择 - 根据操作系统版本自动选择音频源。在此模式下，Windows XP 下通过默认 DirectSound 捕获设备录制音频流；Windows Vista 及更新版本操作系统下，音频源 #1 通过默认 WASAPI 播放设备录制，音频源 #2 通过默认 WASAPI 捕获设备录制。
- DirectSound 捕获设备 - 通过 DirectSound 捕获设备捕获音频流。在 Windows XP 下，DirectSound 捕获设备由硬件加速，因此该模式凭借硬件声音混音和回环录制实现，可提供最佳的音频捕获性能。
然而，自 Windows Vista 起，DirectSound 已被弃用并通过 WASAPI 进行模拟，因此建议在 Windows Vista 及更新版本操作系统下使用原生 WASAPI 设备进行音频捕获。
- WASAPI 播放设备 - 通过 WASAPI 播放设备捕获音频流(仅适用于 Windows Vista 或更新版本操作系统)。与 DirectSound 不同，WASAPI 允许在原始多声道音频流传递给音频驱动程序和播放设备之前将其拦截。这提供了与软件音频驱动程序无关的统一回环录制实现，能够录制多声道音频。
请注意，此模式可能与某些使用 OpenAL 等其他硬件加速音频接口的应用程序不兼容。
- WASAPI 捕获设备 - 通过 WASAPI 捕获设备捕获音频流(仅适用于 Windows Vista 或更新版本操作系统)。与 DirectSound 不同，WASAPI 可以独立捕获输入和输出音频流，这使您能够独立录制来自输出设备(如扬声器)和来自输入设备(如麦克风)的音频流。如有需要，您可以使用此模式录制来自输入设备的音频流。

提示:
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
