帧尺寸
在全帧尺寸的高分辨率下捕获视频时，由于每帧都有大量数据通过总线从 GPU 复制到 CPU，PCIE 总线带宽可能成为限制帧率的瓶颈。
在这种情况下，您可以尝试通过减小帧尺寸来提高性能，例如选择 1/2 帧尺寸而不是全帧尺寸，会将从 GPU 传输到 CPU 的数据量减少 4 倍。减小帧尺寸还可以通过减少要压缩和写入硬盘的数据量，有效降低 CPU 负载和硬盘负载。

提示:
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
