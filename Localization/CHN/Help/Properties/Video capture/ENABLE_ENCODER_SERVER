使用专用编码器服务器
默认情况下，视频编码在所捕获视频的 3D 进程上下文中执行。这意味着从 32 位应用程序捕获视频时只能使用 32 位版本的编解码器，从 64 位应用程序捕获视频时只能使用 64 位版本的编解码器。
您可以启用此选项强制在辅助专用编码器服务进程中执行编码。这样，您就可以在从 64 位应用程序捕获视频时使用 32 位版本的编解码器，反之亦然。
此外，专用编码器服务器在独立的地址空间中运行，因此启用此选项可以为预录制缓冲区保留更多的 RAM，而不会占用您正在捕获视频的 3D 应用程序的地址空间。
在某些特定情况下，由于不同的 DirectX 同步问题导致进程内视频编码性能下降时，专用编码器服务器也很有用。例如，当从 Direct3D9 应用程序捕获视频时，通过在 Direct3D9 硬件加速模式下工作的 Intel QuickSync 插件使用专用视频编码器可以提升编码性能。

提示:
- 此选项需要在 %SERVERPRODUCTNAME% 的兼容性属性中启用专用编码器服务器服务。
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
