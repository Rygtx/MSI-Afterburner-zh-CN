多线程优化
视频压缩引擎经过优化，可充分利用配备多个 CPU 或多核 CPU 的系统。输入帧队列由多个线程同时处理，此选项允许您控制处理队列的线程数量。默认情况下，在配备 N 个 CPU 或 CPU 核心的系统上，视频压缩引擎自动使用 1 个线程将压缩帧刷新到硬盘，使用 N-1 个线程压缩输入队列中的帧。但是，出于故障排除或性能比较的原因，您可以减少线程数量或完全禁用多线程实现。

提示:
- 使用 VFW 压缩时，无论此选项如何，帧都由单个压缩线程处理。但是，在这种情况下，色彩空间转换仍由多个线程执行。
- 您可以在高级属性的<用户界面>选项卡中取消勾选来关闭用户界面工具提示。
