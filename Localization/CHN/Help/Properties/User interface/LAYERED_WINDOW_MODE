皮肤合成模式
允许选择皮肤合成模式。合成模式定义皮肤引擎如何将皮肤化窗口的像素与桌面以及底层窗口的像素进行组合。您可以选择以下合成模式之一:

- 传统 - 提供与旧操作系统和皮肤的向后兼容性。此合成模式使用 GDI 裁剪区域来渲染非矩形窗口形状,因此如果窗口包含过多非矩形区域,窗口更新性能可能会降低。
- 分层(颜色键)- 为现代操作系统提供最佳的皮肤化窗口渲染性能。此合成模式使用支持硬件加速颜色键的分层窗口,因此窗口更新性能不依赖于窗口形状。此外,您可以在此模式下调整皮肤透明度级别。
- 分层(Alpha 通道)- 为皮肤引擎提供附加效果支持。此合成模式使用支持硬件加速每像素 Alpha 通道的分层窗口,因此皮肤设计师可以在皮肤内部定义半透明区域、对皮肤边缘应用抗锯齿等。您也可以在此模式下调整皮肤透明度级别。

提示:
- 您可以通过高级属性中的<用户界面>选项卡禁用用户界面工具提示。
